---
import "../../assets/icons.scss";

const baseURL = import.meta.env.BASE_URL;
const { icon, class: className } = Astro.props;
---

<i
  class:list={["icon-64", icon, className]}
  style=`background-image: url('${baseURL}/img/icons-64.png')`></i>
